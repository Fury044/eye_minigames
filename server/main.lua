-- No required server logic — minigames are client-side skill checks.
-- Add anti-abuse hooks here if needed (e.g. rate-limit paid actions).

print('^2[eye_minigames]^7 loaded — 34 minigames ready.')
